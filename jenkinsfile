pipeline {
  agent any
  stages {
    stage('Install Dependencies') {
      steps {
        sh 'echo "Hello this is backend"'
      }
    }

    stage('Run Migrations') {
      agent any
      
      steps {
        sh 'java -version'
      }
    }

  }
}
